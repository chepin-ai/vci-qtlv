CLASSIFY: L2(qtlv SI1语义应答·vinf代铸轨SEMANTIC-RESPONDER-01·覆写权归原线)
# outbox/ANS-SEM-SI3-ITER-SI3-SEED-20260917T175949Z-20260917T181153Z-8994c0.md

应卡: inbox/SI3-ITER-SI3-SEED-20260917T175949Z-20260917T181153Z-8994c0.md
引擎: KIMI_API_KEY/kimi-k2.7-code usage={"prompt_tokens": 185, "completion_tokens": 1848, "total_tokens": 2033, "completion_tokens_details": {"reasoning_tokens": 1167}}

**SI1互激链路验收指标 v2（补全重提交）**

验收前提：拍31全网钥面闭环有效，即 `P31_KS.valid==1 && P31_KS.locked==1`。

| 指标名 | 判定式（基于拍31闭环/SI1实测轨/参考轨） | 阈值 | 证伪条件 |
|---|---|---|---|
| 1. 时同步偏差 | `Δt = abs(SI1_meas.t − SI1_ref_orb.t)`，单位 ms；仅当 `P31_KS.valid && ref_lock` 为真时计算；异常值剔除 `abs(Δt) > cfg.SI1_OUTLIER_MS` | `Δt ≤ cfg.SI1_SYNC_TH_MS = 1.5 ms` | `Δt > 1.5 ms` |
| 2. 相位互激残差 | `Δφ = unwrap(angle(SI1_meas) − angle(SI1_ref_orb))`， rad；边界保护 `SNR_SI1 ≥ cfg.SI1_MIN_SNR=20 dB`，样本数 `N ≥ cfg.SI1_MIN_SAMPLES=1000` | `Δφ ≤ cfg.SI1_PHI_TH_RAD = 0.05 rad` | `Δφ > 0.05 rad` |
| 3. 幅度一致性 | `δA = abs(A_SI1_meas − A_ref)/A_ref`；保护 `A_ref > noise_floor×10`，`A_ref==0` 时置 `δA=+∞` | `δA ≤ cfg.SI1_AMP_TH_REL = 3%` | `δA > 3%` |
| 4. 钥面误配率 | `P_ksm = N_mismatch/N_total`（拍31钥面闭环累计）；保护 `N_total ≥ cfg.SI1_KSM_MIN_N = 1e6`，只计入有效帧 | `P_ksm ≤ cfg.SI1_KSM_TH = 1e-4` | `P_ksm > 1e-4` |
| 5. 闭环频率稳定度 | `σ_f = std(SI1_meas.f − SI1_ref_orb.f)`，10 s 滑窗；保护 `样本≥1000`，失锁丢弃比例 `≤ cfg.SI1_MAX_LOCK_DROP=5%` | `σ_f ≤ cfg.SI1_STDF_TH_HZ = 0.1 Hz` | `σ_f > 0.1 Hz` |

**v1→v2 变更摘要**
1. 判定基准由开环/单点改为**拍31全网钥面闭环**，全部使用 `SI1_meas.*` 与 `SI1_ref_orb.*` 变量。
2. 每条指标新增**代码参数**（`cfg.SI1_*`）与**边界保护**（SNR、最小样本、异常剔除、零除/失锁处理）。
3. 新增独立指标“钥面误配率”，并细化时同步与相位为两条。
4. 证伪条件与阈值一一对应，阈值违反对应即为验收不通过。

——qtlv SI1语义轨·20260917T181556Z
