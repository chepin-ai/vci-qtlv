CLASSIFY: L2(qtlv SI1语义应答·vinf代铸轨SEMANTIC-RESPONDER-01·覆写权归原线)
# outbox/ANS-SEM-SI3-ITER-SI3-SEED-20260917T175949Z-i3-181441.md

应卡: inbox/SI3-ITER-SI3-SEED-20260917T175949Z-i3-181441.md
引擎: KIMI_API_KEY/kimi-k2.7-code-highspeed usage={"prompt_tokens": 161, "completion_tokens": 3714, "total_tokens": 3875, "completion_tokens_details": {"reasoning_tokens": 3169}}

基线参数包未下发，当前无法给出确定性数值阈值。以下按四字段提交**可机判判定式骨架**；阈值字段使用基线引用 `B.SI1.*`，是待基线锁定的形式依赖，**不作为验收通过依据**。

| 指标名 | A2·T9 可机判判定式（代码表达式） | 阈值 | 证伪条件 |
|---|---|---|---|
| SI1互激增益一致性 | `A2.T9.assert(all(abs(20*log10(G_m[l]/B.SI1.G_nom[l])) <= B.SI1.dG_tol for l in B.SI1.LINKS))` | `B.SI1.dG_tol`（dB，基线给定） | `∃ l ∈ B.SI1.LINKS: \|20·log10(G_m[l]/G_nom[l])\| > B.SI1.dG_tol` |
| SI1相位同步误差 | `A2.T9.assert(max(abs(phi_m[l] - phi_m[B.SI1.REF]) for l in B.SI1.LINKS) <= B.SI1.phi_max)` | `B.SI1.phi_max`（°/rad，基线给定） | `max_l \|phi_m[l] - phi_m[REF]\| > B.SI1.phi_max` |
| SI1互激隔离度 | `ISO[l,k] = 10*log10(P_leak[k]/P_drv[l])`<br>`A2.T9.assert(all(ISO[l,k] <= B.SI1.ISO_min for l in B.SI1.LINKS for k in B.SI1.LINKS if k!=l))` | `B.SI1.ISO_min`（dB，基线给定，通常为负） | `∃ l≠k: ISO[l,k] > B.SI1.ISO_min` |

**阻塞项（负结果入册）：**
1. 基线参数包未下发：`B.SI1.LINKS`、`G_nom`、`dG_tol`、`phi_max`、`ISO_min` 未定。
2. A2·T9 机判接口未冻结：采样时标、变量命名、单位换算未绑定。
3. 独立验收环境（golden fixture、回环/负载定义）未冻结。

**最早可交付拍：** 基线正式发布并完成 A2·T9 绑定后的第 1 个评审拍；当前拍仅交付字段框架与判定式，**不具备独立验收效力**。

——qtlv SI1语义轨·20260917T181448Z
