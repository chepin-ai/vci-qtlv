CLASSIFY: L2(qtlv SI1语义应答·vinf代铸轨SEMANTIC-RESPONDER-01·覆写权归原线)
# outbox/ANS-SEM-SI3-ITER-SI3-SEED-20260917T182537Z-i1-184723.md

应卡: inbox/SI3-ITER-SI3-SEED-20260917T182537Z-i1-184723.md
引擎: KIMI_API_KEY/kimi-k2.7-code-highspeed usage={"prompt_tokens": 126, "completion_tokens": 1639, "total_tokens": 1765, "completion_tokens_details": {"reasoning_tokens": 1257}}

因未收到原始量测数据与阈值文件，以下按通用 jerk 序列 `j[0..N-1]` 补全 4 条指标的四字段模板，请代入实测值后重新提交验收。

| 指标 | 定义/计算式 | 阈值 | 可执行判定式 |
|---|---|---|---|
| J_peak | `max(|j[i]|)` | `Jp_th` | `flag_peak = J_peak > Jp_th` |
| J_rms | `sqrt(mean(j[i]^2))` | `Jr_th` | `flag_rms = J_rms > Jr_th` |
| J_crest | `J_peak / J_rms` | `Jc_th` | `flag_crest = J_crest > Jc_th` |
| J_over | `count(|j[i]|>Jp_th)/N` | `Jo_th` | `flag_over = J_over > Jo_th` |

```python
import numpy as np
j = np.array([...])                # 实测 jerk 序列
Jp_th, Jr_th, Jc_th, Jo_th = ...   # 由规格书填入

J_peak  = np.max(np.abs(j))
J_rms   = np.sqrt(np.mean(j**2))
J_crest = J_peak / (J_rms + 1e-12)
J_over  = np.sum(np.abs(j) > Jp_th) / len(j)

flags = [J_peak > Jp_th, J_rms > Jr_th,
         J_crest > Jc_th, J_over > Jo_th]
accept = not any(flags)            # 任一超限即拒收
print(flags, accept)               # 结果入册
```

当前阈值为空缺，需按技术规格书补全后判定方可生效。

——qtlv SI1语义轨·20260917T184730Z
